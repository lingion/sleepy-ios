// SleepyApp.swift — ← SleepyApp.kt
// Sleepy iOS — 100% port of sleepy Android (~/Desktop/sleepy), GPL-3.0
// D8 批次落地完整生命周期逻辑;D0 为可编译占位壳。

import SwiftUI
import GRDB

@main
struct SleepyApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Sleepy — D0 scaffold")
        }
    }
}
